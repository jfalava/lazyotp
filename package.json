{
  "name": "lazyotp",
  "version": "0.2.3",
  "private": true,
  "bin": {
    "lazyotp": "./index.ts"
  },
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "build": "mkdir -p dist && bun build --compile index.ts --outfile dist/lazyotp",
    "build:all": "bun run build:darwin-arm64 && bun run build:darwin-x64 && bun run build:linux-x64 && bun run build:linux-arm64 && bun run build:windows-x64",
    "build:darwin-arm64": "mkdir -p dist && bun build --compile --target=bun-darwin-arm64 index.ts --outfile dist/lazyotp-darwin-arm64",
    "build:darwin-x64": "mkdir -p dist && bun build --compile --target=bun-darwin-x64 index.ts --outfile dist/lazyotp-darwin-x64",
    "build:linux-arm64": "mkdir -p dist && bun build --compile --target=bun-linux-arm64 index.ts --outfile dist/lazyotp-linux-arm64",
    "build:linux-x64": "mkdir -p dist && bun build --compile --target=bun-linux-x64 index.ts --outfile dist/lazyotp-linux-x64",
    "build:windows-x64": "mkdir -p dist && bun build --compile --target=bun-windows-x64 index.ts --outfile dist/lazyotp-windows-x64.exe",
    "check": "oxlint --fix",
    "format": "oxfmt",
    "lint": "oxlint",
    "otp": "bun run index.ts",
    "start": "bun run index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "7.0.2"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.81.0",
    "@types/node": "26.4.1",
    "bun-types": "1.4.1",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "oxlint-tsgolint": "7.0.2001",
    "vitest": "5.0.0"
  }
}
